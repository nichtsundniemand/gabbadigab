/* TODO (therufuser#8#): Get the item system done
    -add icons (should be done in RoomRenderer/inventoryRenderer separately)
        -itemcatalog:
            -holds item definition (stacksize, )
            -loads the a catalog file... great fun, it will be!
            -holds icons and 3d/sprite data

    -how will I render them in the room?
    -can't even (use them)
*/

/* TODO (therufuser#4#): Rooms
    -needs some implementation of maps
    -more?

    -want item dropping
    ->item space, room space
*/

/* TODO (therufuser#7#): Add Font Cache
    -Pre-Render every possible glyph upon initialization
    -If a glyph is not availabe-> On-Demand rendering
    -gotta free the Surface yourself
*/

// TODO (therufuser#7#): Character
// TODO (therufuser#3#): Proper Input
